import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function TradesPage() {
  return (
    <PlaceholderPage
      title="Trades"
      description="Your trade log will live here — a searchable, filterable table of every position."
    />
  );
}
